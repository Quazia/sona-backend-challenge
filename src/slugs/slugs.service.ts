import { Injectable } from '@nestjs/common';

@Injectable()
export class SlugsService {
  create(url: string) {
    // Generate slug
    // Push slug+URL to db
    return null;
  }
}

// Path: src\slugs\slug.service.ts
